using Global.Requirements.DistancePassed;
using Global.Requirements.DistancePassed.First;
using UnityEngine;

namespace Specifications.Requirements.DistancePassed
{
    [CreateAssetMenu(menuName = "Create Specification/New DistancePassed Requirement/First", fileName = nameof(FirstDistancePassedRequirement), order = 52)]
    public class FirstDistancePassedRequirementSpecification : BaseRequirementSpecification<FirstDistancePassedRequirement>
    {
    }
}